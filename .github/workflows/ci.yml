name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build & test MaximizeCore
    # Linux, not macOS, and the architecture guard below is what makes that legal.
    # `MaximizeCore` is forbidden from importing SwiftUI, UIKit, HealthKit, SwiftData,
    # CloudKit, MapKit, Charts or Security, so it is pure Swift and Foundation and has
    # no reason to need a Mac. On a private repo the difference is 10x: macOS minutes
    # bill at a 10x multiplier against the included allowance, Linux at 1x, and this is
    # the job that runs on every commit. Only the app-shell job below genuinely needs
    # macOS, because only it runs `xcodebuild`.
    #
    # `platforms:` in Package.swift sets minimum *Apple* versions; it does not exclude
    # Linux, and nothing in the core is Apple-only. If that ever stops being true, this
    # job fails loudly — which is the correct outcome, because it would mean the core
    # had grown a platform dependency the guard was supposed to prevent.
    runs-on: ubuntu-latest
    container: swift:6.0
    # Inside a container GitHub defaults `shell:` to `sh`, not `bash`, and the build
    # steps below use `set -o pipefail` — a bashism that `sh` rejects outright with
    # "Illegal option -o pipefail" before the compiler is ever invoked. Setting it once
    # here beats remembering it per step.
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      # swift-corelibs-foundation reads real time-zone data for anything involving a
      # named zone. Several tests turn a civil day into an instant in America/New_York
      # or Asia/Kathmandu, so a missing tzdata would fail them on the zone rather than
      # on anything real.
      #
      # `swift:6.0` turns out to already carry it — this is insurance against an image
      # that stops doing so, and it costs one apt index fetch.
      - name: Install time zone data
        run: apt-get update && apt-get install -y --no-install-recommends tzdata

      - name: Show toolchain
        run: swift --version

      # Output is teed so the failure step below can find the diagnostics. A Swift
      # build emits hundreds of warning lines, and a compiler error lands somewhere in
      # the middle of them — so a failed run's log tail is almost always warnings and
      # cleanup, with the one line that matters scrolled out of reach.
      - name: Build
        run: |
          set -o pipefail
          swift build --build-tests 2>&1 | tee /tmp/swift-build.log

      - name: Test
        run: |
          set -o pipefail
          swift test 2>&1 | tee /tmp/swift-test.log

      # Puts the diagnostics in the job summary, where they are the first thing visible
      # rather than something to go hunting for.
      - name: Surface compiler errors and test failures
        if: failure()
        run: |
          {
            echo "### Diagnostics"
            echo
            echo '```'
            grep -hE "error:|XCTAssert.*failed|failed \\(" /tmp/swift-build.log /tmp/swift-test.log 2>/dev/null | head -40 \
              || echo "No error: lines found — the failure was not a compile or assertion error."
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          echo "--- diagnostics ---"
          grep -hE "error:|XCTAssert.*failed|failed \\(" /tmp/swift-build.log /tmp/swift-test.log 2>/dev/null | head -40 || true

  architecture:
    name: Core stays platform-free
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # MaximizeCore must not depend on UI, persistence, or platform frameworks; that
      # is what keeps the scoring and metrics logic testable in CI without a
      # simulator or a device. See CLAUDE.md.
      - name: Reject platform imports in MaximizeCore
        run: |
          # Security is on this list because of MAX-022: the core defines the
          # AnthropicAPIKeyStoring protocol, and the Keychain implementation lives in
          # the app layer. Without the guard, nothing stops a later ticket from
          # reaching for SecItemCopyMatching directly in the core and quietly making
          # the key-handling path untestable again.
          BANNED='SwiftUI|UIKit|HealthKit|SwiftData|CloudKit|MapKit|Charts|Security'
          if grep -rEn "^\s*import\s+($BANNED)\b" Sources/MaximizeCore; then
            echo "::error::MaximizeCore must not import platform frameworks ($BANNED)."
            echo "Define a protocol or value type in the core; implement it in the app layer."
            exit 1
          fi
          echo "OK: no platform framework imports in MaximizeCore."

      # MAX-040's design system is only worth having if views name meaning
      # (Color.textSecondary) rather than appearance. One stray literal is harmless;
      # the habit is what erodes a palette, and it erodes quietly — nothing breaks,
      # the colors just stop agreeing with each other.
      #
      # MAX-070 moved the palette values into MaximizeCore as DesignPalette so their
      # WCAG contrast could be computed and tested without UIKit, leaving App/
      # entirely literal-free. So this guard now admits no exceptions: a color value
      # anywhere under App/ is a value escaping the contrast suite that guards it.
      - name: Reject raw color literals in the app layer
        run: |
          HITS=$(grep -rEn '0x[0-9A-Fa-f]{6}|Color\(red:|UIColor\(red:|Color\(\.sRGB' \
            --include='*.swift' App || true)
          if [ -n "$HITS" ]; then
            echo "$HITS"
            echo "::error::Raw color values belong in Sources/MaximizeCore/Accessibility/DesignPalette.swift."
            echo "Define it there so WCAGContrastTests covers it, then expose a semantic token in ColorTokens.swift."
            exit 1
          fi
          echo "OK: no raw color literals anywhere in App/."

  ios-app:
    name: Build app shell for iOS Simulator
    # macos-26 (Tahoe) has been GA on GitHub-hosted runners since Feb 2026 and is the
    # natural home for iOS 26 SDK work — PRD §7.4 wants Liquid Glass, which needs
    # Xcode 26. Independent of this runner's default Xcode, the next step verifies a
    # 26.x toolchain is actually present and fails loudly if it isn't, rather than
    # silently building against an older SDK.
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 26 (iOS 26 SDK, required for PRD §7.4 Liquid Glass)
        run: |
          XCODE_26=$(ls -d /Applications/Xcode_26*.app 2>/dev/null | sort -V | tail -1)
          if [ -z "$XCODE_26" ]; then
            echo "::error::No Xcode 26.x found on this runner. PRD §7.4 requires the iOS 26 SDK for Liquid Glass; refusing to silently fall back to an older SDK."
            echo "Available Xcode installs:"
            ls /Applications | grep -i xcode || true
            exit 1
          fi
          echo "Selecting $XCODE_26"
          sudo xcode-select -s "$XCODE_26/Contents/Developer"

      - name: Show toolchain
        run: xcodebuild -version

      - name: Install XcodeGen
        run: HOMEBREW_NO_AUTO_UPDATE=1 brew install xcodegen

      - name: Generate Xcode project from project.yml
        run: xcodegen generate

      # Unsigned build against a generic simulator destination — no specific
      # device/OS pinned, so this doesn't depend on which simulator runtimes happen
      # to be pre-provisioned on the runner. This proves the app target compiles and
      # links MaximizeCore; it does not run the app or verify anything visually.
      # Teed for the same reason the core job tees its build: xcodebuild prints the
      # whole compile graph, and a failed run's log tail is the *last* files it was
      # working on, not the one that failed. The error itself lands thousands of lines
      # earlier and is effectively unreachable without this.
      - name: Build for iOS Simulator (unsigned)
        run: |
          set -o pipefail
          xcodebuild build \
            -project Maximize.xcodeproj \
            -scheme Maximize \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO 2>&1 | tee /tmp/xcodebuild.log

      # Puts the diagnostics in the job summary, where they are the first thing visible
      # rather than something to go hunting for. The two lines after each error are kept
      # because Swift prints the offending source line and a caret underneath it, and
      # the message alone is often ambiguous without them.
      - name: Surface compiler errors
        if: failure()
        run: |
          {
            echo "### Diagnostics"
            echo
            echo '```'
            grep -E -A 2 "error:" /tmp/xcodebuild.log 2>/dev/null | head -60 \
              || echo "No error: lines found — the failure was not a compile error."
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          echo "--- diagnostics ---"
          grep -E -A 2 "error:" /tmp/xcodebuild.log 2>/dev/null | head -60 || true
